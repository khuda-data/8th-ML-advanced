# pytest configuration
[tool:pytest]
testpaths = tests env/entities/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    unit: unit tests
    integration: integration tests
    slow: slow running tests
